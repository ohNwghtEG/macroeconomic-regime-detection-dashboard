# Copy to .env and fill in your own free key from
# https://fred.stlouisfed.org/docs/api/api_key.html
# NOTE: the project runs fully offline from the committed parquet cache
# without a key. A key is only needed to refresh the data.
FRED_API_KEY=your_key_here
